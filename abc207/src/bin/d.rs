use proconio::input;

fn dfs(i: usize, memo: &mut Vec<Vec<i32>>, count: &mut usize)

fn main() {
    input! {
        n: usize,
        ab: [(i32, i32); n],
        cd: [(i32, i32); n],
    }

    let mut count = vec![vec![0; n + 1]; n];
    for i in (1..n).rev() {
        for j in 0..(i - 1)
    }
}
